# Dataset (large, not distributed via git — see README for source)
data/

# Model weights (large binaries — published as GitHub Release assets instead)
*.pt
*.pth
*.onnx

# YOLO/ultralytics run outputs (regenerated by training/inference; keep out of history)
runs/*/weights/

# Jupyter / Python
.ipynb_checkpoints/
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
